import 'package:flutter/material.dart';

class VictoriaFalls extends StatelessWidget {
  const VictoriaFalls({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Victoria Falls")),
      body: const Center(child: Text("Victoria Falls Page")),
    );
  }
}
