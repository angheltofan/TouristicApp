import 'package:flutter/material.dart';

class TableMountain extends StatelessWidget {
  const TableMountain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Table Mountain")),
      body: const Center(child: Text("Table Mountain Page")),
    );
  }
}
