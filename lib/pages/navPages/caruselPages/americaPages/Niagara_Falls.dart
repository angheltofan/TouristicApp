import 'package:flutter/material.dart';

class NiagaraFalls extends StatelessWidget {
  const NiagaraFalls({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Niagara Falls")),
      body: const Center(child: Text("Niagara Falls Page")),
    );
  }
}
