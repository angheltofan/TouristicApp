import 'package:flutter/material.dart';

class AyersRock extends StatelessWidget {
  const AyersRock({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Ayers Rock")),
      body: const Center(child: Text("Ayers Rock Page")),
    );
  }
}
