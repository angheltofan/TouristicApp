import 'package:flutter/material.dart';

class GreatoceanroadPage extends StatelessWidget {
  const GreatoceanroadPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Great Ocean Road")),
      body: const Center(child: Text("Great Ocean Road Page")),
    );
  }
}
