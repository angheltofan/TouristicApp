import 'package:flutter/material.dart';

class HobartPage extends StatelessWidget {
  const HobartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Hobart")),
      body: const Center(child: Text("Hobart Page")),
    );
  }
}
