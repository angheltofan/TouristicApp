import 'package:flutter/material.dart';

class GoldenPavilionPage extends StatelessWidget {
  const GoldenPavilionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Golden Pavilion")),
      body: const Center(child: Text("Golden Pavilion Page")),
    );
  }
}
