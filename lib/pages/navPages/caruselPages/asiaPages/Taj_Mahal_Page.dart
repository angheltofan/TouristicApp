import 'package:flutter/material.dart';

class TajMahalPage extends StatelessWidget {
  const TajMahalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Taj Mahal")),
      body: const Center(child: Text("Taj Mahal Page")),
    );
  }
}
